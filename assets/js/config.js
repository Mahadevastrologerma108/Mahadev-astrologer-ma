// config.js
const AppConfig = {
    // Sirf yahan file name change karna hoga future updates ke liye
    dataFiles: {
        events: "data/events-2026.json", 
        panchang: "data/panchang-feb-2026.json"
    },
    currentLanguage: 'en' // Default
};
